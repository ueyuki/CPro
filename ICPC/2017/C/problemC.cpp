#include <bits/stdc++.h>
using namespace std;

int main() {
  int d, w;
  cin >> d >> w;
  e[d][w];

  for (int k = 0; k < d; k++) {
    for (int l = 0; l < w; l++) {
      cin >> e[k][l];
    }
  }

  for (int x = 1; x < d-1; x++) {
    for (int y = 1; y < w-1; y++) {
      for (int dx = 0; dx < w-x; dx++;) {
        for (int dy = 0; dy < w-y; dy++;) {
        }
      }
    }
  }
}
