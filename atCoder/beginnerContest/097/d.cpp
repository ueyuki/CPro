#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m;
  int ans = 0;
  vector<int> p;
  cin >> n >> m;
  int pair[m][m];
  p.push_back(t);
  for (int k = 1; k <= n; k++) {
    int t;
    cin >> t;
    if (k == t) ans++;
    p.push_back(t);
  }
  for (int k = 0; k < m; k++) {
    cin >> pair[k][k];
  }

}
